import React from "react";

import { LoginForm } from "./LoginForm";

export default {
  title: "LoginForm",
};

export const LoginFormStory: React.FC<{}> = () => {
  return (
    <LoginForm/>
  );
};